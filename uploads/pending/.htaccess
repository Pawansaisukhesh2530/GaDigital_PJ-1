# Temporary attachment staging for EMAIL_ONLY applications.
# These files are read server-side only and must never be web-accessible.
Require all denied
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
