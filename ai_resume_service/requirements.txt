# Milestone 1 — Resume Text Extraction
# Milestone 2 — Text Normalization & Section Detection (stdlib only, no new deps)
# Only the libraries required for these milestones are listed here.
# Do NOT add PaddleOCR, PaddlePaddle, OpenCV, Ollama, Qwen, or Gemini SDKs yet.

fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20
PyMuPDF==1.25.1
pdfplumber==0.11.4
python-docx==1.1.2
Pillow==11.0.0
python-dotenv==1.0.1
